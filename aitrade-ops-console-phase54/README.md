# AITRADE Operations Console · Phase 54 (visual mock)

This folder is the original UI mock. The integrated console (real AITRADE adapters) is:

`dashboard/ops-console/`

```text
python dashboard/ops-console/api.py
```

Connection is **not** execution permission. `ORDER EXECUTION DISABLED` is persistent.
